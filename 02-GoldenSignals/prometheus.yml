global:
  scrape_interval: 5s

scrape_configs:
  - job_name: "GoldenSignals"
    static_configs:
      - targets:
        - "localhost:5000"
        - "localhost:9100"

rule_files:
  - alert_rules.yml
